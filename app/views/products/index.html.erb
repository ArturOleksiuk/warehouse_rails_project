<div class="d-flex justify-content-between align-items-center mb-4">
  <h1 class="h3">📦 Список товарів</h1>
  <%= link_to "Додати новий товар", new_product_path, class: "btn btn-success" %>
</div>

<div class="card shadow-sm">
  <div class="card-body">
    <div class="table-responsive">
      <table class="table table-hover align-middle">
        <thead class="table-light">
          <tr>
            <th>Назва</th>
            <th>Категорія</th>
            <th>Постачальник</th>
            <th>Ціна</th>
            <th>Кількість</th>
            <th class="text-end">Дії</th>
          </tr>
        </thead>
        <tbody>
          <% @products.each do |product| %>
            <tr>
              <td class="fw-bold"><%= product.name %></td>
              
              <td>
                <% if product.category %>
                  <span class="badge bg-info text-dark"><%= product.category.name %></span>
                <% else %>
                  <span class="text-muted">-</span>
                <% end %>
              </td>

              <td><%= product.supplier ? product.supplier.company_name : "-" %></td>
              
              <td><%= number_to_currency(product.price, unit: " грн", format: "%n %u") %></td>
              
              <td>
                <% if product.quantity.to_i < 5 %>
                  <span class="badge bg-danger"><%= product.quantity %> (Мало!)</span>
                <% else %>
                  <span class="badge bg-success"><%= product.quantity %></span>
                <% end %>
              </td>

              <td class="text-end">
                <div class="btn-group" role="group">
                  <%= link_to "Огляд", product, class: "btn btn-sm btn-outline-primary" %>
                  <%= link_to "Ред.", edit_product_path(product), class: "btn btn-sm btn-outline-secondary" %>
                  <%= link_to "Вид.", product, data: { turbo_method: :delete, turbo_confirm: "Ви впевнені?" }, class: "btn btn-sm btn-outline-danger" %>
                </div>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
