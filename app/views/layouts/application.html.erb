<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Lab4" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Lab4">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
      <div class="container">
        <a class="navbar-brand" href="/">📦 Склад</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
          
          <ul class="navbar-nav me-auto">
            <li class="nav-item"><%= link_to "Про нас", about_path, class: "nav-link" %></li>
            <li class="nav-item"><%= link_to "Контакти", contact_path, class: "nav-link" %></li>

            <% if user_signed_in? %>
              <li class="nav-item border-start ms-2 ps-2"><%= link_to "Товари", products_path, class: "nav-link" %></li>
              <li class="nav-item"><%= link_to "Категорії", categories_path, class: "nav-link" %></li>
              <li class="nav-item"><%= link_to "Постачальники", suppliers_path, class: "nav-link" %></li>
              <li class="nav-item"><%= link_to "Клієнти", customers_path, class: "nav-link" %></li>
            <% end %>
          </ul>

          <ul class="navbar-nav ms-auto">
            <% if user_signed_in? %>
              <li class="nav-item">
                <span class="nav-link text-light">👤 <%= current_user.email %></span>
              </li>
              <li class="nav-item">
                <%= button_to "Вийти", destroy_user_session_path, method: :delete, class: "btn btn-outline-danger btn-sm mt-1 ms-2" %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "Увійти", new_user_session_path, class: "btn btn-outline-light btn-sm mt-1 me-2" %>
              </li>
              <li class="nav-item">
                <%= link_to "Реєстрація", new_user_registration_path, class: "btn btn-warning btn-sm mt-1" %>
              </li>
            <% end %>
          </ul>
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to 'UK', url_for(locale: :uk), class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to 'EN', url_for(locale: :en), class: 'nav-link' %>
            </li>
<         /ul>
        </div>
      </div>
    </nav>

    <div class="container">
      <% flash.each do |type, msg| %>
        <% alert_class = type == "alert" ? "alert-danger" : "alert-success" %>
        <div class="alert <%= alert_class %> alert-dismissible fade show" role="alert">
          <%= msg %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>

      <%= yield %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>