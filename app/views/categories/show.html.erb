<div class="row justify-content-center">
  <div class="col-lg-8">
    
    <div class="card shadow-sm mb-4">
      <div class="card-header bg-warning text-dark d-flex justify-content-between align-items-center">
        <h3 class="mb-0"><%= @category.name %></h3>
        <span class="badge bg-dark text-white">Категорія</span>
      </div>
      <div class="card-body">
        <p class="fs-5"><%= @category.description.presence || "Опис відсутній" %></p>
        
        <div class="d-flex gap-2 mt-4">
          <%= link_to "Редагувати", edit_category_path(@category), class: "btn btn-outline-dark" %>
          <%= link_to "До списку", categories_path, class: "btn btn-outline-secondary" %>
        </div>
      </div>
    </div>

    <div class="card shadow-sm">
      <div class="card-header bg-light">
        <h5 class="mb-0">Товари в цій категорії (<%= @category.products.count %>)</h5>
      </div>
      <% if @category.products.any? %>
        <ul class="list-group list-group-flush">
          <% @category.products.each do |product| %>
            <li class="list-group-item d-flex justify-content-between align-items-center">
              <div>
                <strong><%= link_to product.name, product_path(product), class: "text-decoration-none text-dark" %></strong>
                <br>
                <small class="text-muted">Ціна: <%= product.price %> грн</small>
              </div>
              <span class="badge bg-success rounded-pill"><%= product.quantity %> шт.</span>
            </li>
          <% end %>
        </ul>
      <% else %>
        <div class="card-body text-center text-muted py-4">
          У цій категорії поки що немає товарів.
          <br>
          <%= link_to "Додати перший товар", new_product_path(category_id: @category.id), class: "btn btn-sm btn-primary mt-2" %>
        </div>
      <% end %>
    </div>

  </div>
</div>
